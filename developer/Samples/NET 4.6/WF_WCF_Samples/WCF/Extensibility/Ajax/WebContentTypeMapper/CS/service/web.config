<?xml version="1.0"?>

<configuration>
  <!-- WCF configuration -->
  <system.serviceModel>
    <standardEndpoints>
      <webHttpEndpoint>
        <!-- The JSON content type mapper is configured. Remove the contentTypeMapper attribute
        to verify; this should result in JCTMClientPage.htm breaking, as expected. -->
        <standardEndpoint name="" contentTypeMapper="Microsoft.Samples.WebContentTypeMapper.JsonContentTypeMapper, JsonContentTypeMapper, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null" />
      </webHttpEndpoint>
    </standardEndpoints>
  </system.serviceModel>
</configuration>