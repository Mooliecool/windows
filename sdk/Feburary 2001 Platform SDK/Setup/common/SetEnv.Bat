@ECHO OFF
REM -------------------------------------------------------------------
REM File    : SetEnv.Bat
REM -------------------------------------------------------------------

ECHO ERROR: This batch file, setenv.bat is a placeholder for a batch file that
ECHO        is generated by the Platform SDK installation process. If you see this
ECHO        message, a problem occurred during setup.
ECHO        To work around this problem copy the file [mssdk]\setup\setenv.tmpl to
ECHO        [mssdk]\setenv.bat and replace every instance of "[PSDKDIR]" with
ECHO        the name of the Platform SDK root installation directory.