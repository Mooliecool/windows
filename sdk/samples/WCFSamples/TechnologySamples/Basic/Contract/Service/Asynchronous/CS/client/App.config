<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <system.serviceModel>
    <client>
      <endpoint address="http://localhost:8000/ServiceModelSamples/service"
                binding="wsHttpBinding"
                contract="Microsoft.ServiceModel.Samples.ICalculator" >
        <!--When running sample in cross machine, then enable the identity tag and specify the identity 
        under which the service is running-->
        <!--<identity>
          <userPrincipalName value="username@fqdn_of_service_machine" />
        </identity>-->
      </endpoint>
    </client>
  </system.serviceModel>

</configuration>
