<Window  
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"    
  x:Class="SDKSample.Window1"
  Title="DetectChangedText_vb"
>
  <StackPanel>

    <TextBox
      Name="tbCounterText"
      IsReadOnly="True"
    >
      0
    </TextBox>

    <TextBox
      Name="tbCountingChanges"    
      TextChanged="textChangedEventHandler"
      TextWrapping="Wrap"
    >
      Here is the initial text in the textbox.  Each time the contents of this box are changed, a change counter will
      be incremented and displayed in a separate TextBox control.  Note that the TextChanged event is called when the TextBox
      control is initially populated with text, so the changes counter starts of at 1.  
    </TextBox>
    
  </StackPanel> 

</Window>
